# Backend Environment Variables
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=  # Leave empty if no password, or add your MySQL password
DB_NAME=expense_analyzer_db
DB_PORT=3306

# JWT Secret
JWT_SECRET=tu_bca_project_2024_expense_analyzer_secret_key_keep_safe

# Server Settings
CORS_ORIGIN=http://localhost:3000